---
phase: 02-safe-additions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vllm/thought_eviction/orchestrator.py
  - vllm/thought_eviction/segmenter.py
  - vllm/sampling_params.py
  - vllm/v1/engine/__init__.py
  - vllm/v1/core/sched/output.py
  - vllm/outputs.py
autonomous: true
requirements: [MERGE-01, MERGE-02, MERGE-03, MERGE-04, MERGE-05, MERGE-06, MERGE-07, MERGE-08]

must_haves:
  truths:
    - "All eviction modules exist and are import-clean against v0.19 (no `import re`, no missing v0.19 imports)"
    - "SamplingParams has all v0.19 upstream fields AND eviction fields in correct position"
    - "EngineCoreOutput has all v0.19 features (PauseMode, EEPNotificationType, REPETITION, etc.) AND new_l2_norms as last field"
    - "EngineCoreRequestType has WAKEUP at b'\\x05' AND UPDATE_MASK at b'\\x06'"
    - "SchedulerOutput has both new_block_ids_to_zero AND evictable_token_ranges_map"
    - "RequestOutput has new_l2_norms field and STREAM_FINISHED sentinel restored"
  artifacts:
    - path: "vllm/thought_eviction/orchestrator.py"
      provides: "Eviction orchestrator with import regex as re"
      contains: "import regex as re"
    - path: "vllm/thought_eviction/segmenter.py"
      provides: "Thought segmenter with import regex as re"
      contains: "import regex as re"
    - path: "vllm/sampling_params.py"
      provides: "SamplingParams with v0.19 fields + eviction fields"
      contains: "enable_l2_norms"
    - path: "vllm/v1/engine/__init__.py"
      provides: "Engine types with v0.19 features + eviction additions"
      contains: "UPDATE_MASK"
    - path: "vllm/v1/core/sched/output.py"
      provides: "SchedulerOutput with both upstream and eviction fields"
      contains: "evictable_token_ranges_map"
    - path: "vllm/outputs.py"
      provides: "RequestOutput with new_l2_norms"
      contains: "new_l2_norms"
  key_links:
    - from: "vllm/v1/engine/__init__.py"
      to: "vllm/sampling_params.py"
      via: "EngineCoreRequest references SamplingParams"
      pattern: "sampling_params.*SamplingParams"
    - from: "vllm/v1/engine/__init__.py"
      to: "vllm/v1/core/sched/output.py"
      via: "EngineCoreRequestType used by SchedulerOutput consumers"
      pattern: "UPDATE_MASK.*b\"\\\\x06\""
---

<objective>
Reconcile eviction modules and struct field additions against v0.19 base.

Purpose: The merge resolution incorrectly picked v0.14 versions of shared files, removing v0.19 features (PauseMode, WAKEUP, RepetitionDetectionParams, new_block_ids_to_zero, etc.) while adding eviction code. This plan restores all v0.19 features and correctly layers eviction additions on top. Also fixes import violations in eviction modules.

Output: All eviction modules import-clean; all shared struct files have complete v0.19 features + correctly positioned eviction fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-groundwork-audit/01-AUDIT-FINDINGS.md
@.planning/phases/02-safe-additions/02-CONTEXT.md
@.planning/phases/02-safe-additions/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix eviction module imports and reconcile sampling_params.py + outputs.py</name>
  <files>
    vllm/thought_eviction/orchestrator.py,
    vllm/thought_eviction/segmenter.py,
    vllm/sampling_params.py,
    vllm/outputs.py
  </files>
  <read_first>
    vllm/thought_eviction/orchestrator.py,
    vllm/thought_eviction/segmenter.py,
    vllm/sampling_params.py,
    vllm/outputs.py
  </read_first>
  <action>
**Step 1: Fix import violations in eviction modules (MERGE-01, per D-03)**

In `vllm/thought_eviction/orchestrator.py`:
- Change `import re` to `import regex as re`
- This is the ONLY change needed; the rest of the file is clean.

In `vllm/thought_eviction/segmenter.py`:
- Change `import re` to `import regex as re`
- This is the ONLY change needed; the rest of the file is clean.

**Step 2: Reconcile sampling_params.py (MERGE-04, per D-08)**

Strategy: Start from v0.19.0 version (`git show v0.19.0:vllm/sampling_params.py`), then add eviction fields.

The current branch has these regressions vs v0.19:
- MISSING: `RepetitionDetectionParams` class (lines 110-147 in v0.19)
- MISSING: `thinking_token_budget` field
- MISSING: `repetition_detection` field
- MISSING: `_eos_token_id` field
- MISSING: `logit_bias`, `allowed_token_ids`, `extra_args`, `bad_words`, `_bad_words_token_ids`, `skip_reading_prefix_cache` fields
- MISSING: `flat_logprobs` field
- MISSING: `dict=True` in class declaration
- PRESENT but stale: `logits_processors` field (removed in v0.19)
- PRESENT but stale: `truncate_prompt_tokens` field (removed in v0.19)
- PRESENT but stale: `disable_fallback` on `StructuredOutputsParams` (not in v0.19)

The correct approach: Write the v0.19.0 version of the file, then append eviction fields after `repetition_detection` and before `output_text_buffer_length`:

```python
# After repetition_detection (last v0.19 upstream field at ~line 291):
repetition_detection: RepetitionDetectionParams | None = None
# --- Eviction fields (D-08) ---
enable_l2_norms: bool = False
l2_norm_layers: list[int] | None = None
# --- Internal post_init fields ---
output_text_buffer_length: int = 0
_eos_token_id: int | None = None
_all_stop_token_ids: set[int] = msgspec.field(default_factory=set)
```

Remove stale v0.14 fields: `logits_processors`, `truncate_prompt_tokens`, `disable_fallback` on StructuredOutputsParams.
Restore all v0.19 imports: `json as json_mod`, `vllm.envs as envs`, `ModelConfig`, `SpeculativeConfig`, `StructuredOutputsConfig`, `is_mistral_tokenizer`. Remove stale v0.14 import: `LogitsProcessor`, `Annotated`.

**Step 3: Reconcile outputs.py (MERGE-08, per D-11)**

The current branch has:
- `new_l2_norms` correctly added (KEEP)
- `multi_modal_placeholders` added (NOT in v0.19, REMOVE)
- `STREAM_FINISHED` sentinel removed (EXISTS in v0.19, RESTORE)

Write v0.19.0 version of the file, then add ONLY `new_l2_norms` as a parameter + attribute:
- Add `new_l2_norms: list[float] | None = None` parameter after `num_cached_tokens` in `__init__`
- Add `self.new_l2_norms = new_l2_norms` in `__init__` body
- Remove `multi_modal_placeholders` import, parameter, and attribute
- Restore `STREAM_FINISHED` sentinel constant
  </action>
  <verify>
    <automated>cd /home/users/ntu/hong0259/vllm-thought-eviction && python -c "
import regex as re
# Verify eviction modules import cleanly
from vllm.thought_eviction import orchestrator, segmenter, strategies, block_utils
# Verify sampling_params has correct fields
from vllm.sampling_params import SamplingParams, RepetitionDetectionParams
sp = SamplingParams()
assert hasattr(sp, 'enable_l2_norms'), 'missing enable_l2_norms'
assert hasattr(sp, 'l2_norm_layers'), 'missing l2_norm_layers'
assert hasattr(sp, 'thinking_token_budget'), 'missing thinking_token_budget'
assert hasattr(sp, 'repetition_detection'), 'missing repetition_detection'
assert not hasattr(sp, 'logits_processors'), 'stale logits_processors present'
assert not hasattr(sp, 'truncate_prompt_tokens'), 'stale truncate_prompt_tokens present'
# Verify outputs.py
from vllm.outputs import RequestOutput, STREAM_FINISHED
print('Task 1 verification passed')
" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `vllm/thought_eviction/orchestrator.py` contains `import regex as re` and does NOT contain bare `import re` (without `regex`)
    - `vllm/thought_eviction/segmenter.py` contains `import regex as re` and does NOT contain bare `import re` (without `regex`)
    - `vllm/sampling_params.py` contains `class RepetitionDetectionParams`
    - `vllm/sampling_params.py` contains `thinking_token_budget: int | None = None`
    - `vllm/sampling_params.py` contains `repetition_detection: RepetitionDetectionParams | None = None`
    - `vllm/sampling_params.py` contains `enable_l2_norms: bool = False` AFTER `repetition_detection` line
    - `vllm/sampling_params.py` contains `l2_norm_layers: list[int] | None = None` AFTER `enable_l2_norms` line
    - `vllm/sampling_params.py` does NOT contain `logits_processors`
    - `vllm/sampling_params.py` does NOT contain `truncate_prompt_tokens`
    - `vllm/sampling_params.py` contains `dict=True` in class declaration
    - `vllm/outputs.py` contains `self.new_l2_norms`
    - `vllm/outputs.py` contains `STREAM_FINISHED`
    - `vllm/outputs.py` does NOT contain `multi_modal_placeholders`
  </acceptance_criteria>
  <done>Eviction modules import-clean; sampling_params.py has all v0.19 fields + eviction fields in correct position per D-08; outputs.py has new_l2_norms + restored STREAM_FINISHED sentinel</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile engine __init__.py and SchedulerOutput</name>
  <files>
    vllm/v1/engine/__init__.py,
    vllm/v1/core/sched/output.py
  </files>
  <read_first>
    vllm/v1/engine/__init__.py,
    vllm/v1/core/sched/output.py
  </read_first>
  <action>
**Step 1: Reconcile vllm/v1/engine/__init__.py (MERGE-05, MERGE-06, per D-09, D-12)**

Strategy: Start from v0.19.0 version (`git show v0.19.0:vllm/v1/engine/__init__.py`), then add eviction additions.

The current branch has these regressions vs v0.19:
- MISSING: `PauseMode` type alias (Literal["abort", "wait", "keep"])
- MISSING: `EEP_NOTIFICATION_CALL_ID = -1`
- MISSING: `EEPNotificationType` enum class
- MISSING: `FINISH_REASON_STRINGS` with "repetition" entry
- MISSING: `FinishReason.REPETITION = 4`
- MISSING: `WAKEUP = b"\x05"` (replaced by UPDATE_MASK at same byte)
- MISSING: `resumable: bool = False` on `EngineCoreRequest`
- MISSING: `reasoning_ended: bool | None = None` on `EngineCoreRequest`
- MISSING: `num_external_computed_tokens: int = 0` on `EngineCoreOutput`
- MISSING: `new_data_parallel_master_port_list` on `ReconfigureDistributedRequest`
- MISSING: `coord_store_port` on `ReconfigureDistributedRequest`
- MISSING: `Literal` import from typing
- PRESENT but stale: `eos_token_id: int | None` on `EngineCoreRequest` (removed in v0.19)
- PRESENT but wrong byte: `UPDATE_MASK = b'\x05'` (must be `b'\x06'`)
- PRESENT but stale: `UPDATE_MASK_REQUEST_TYPE` module-level alias (keep this, just fix byte)

Write the v0.19.0 version of the file, then add these eviction additions:

1. In `EngineCoreRequestType` enum, after `WAKEUP = b"\x05"`, add:
```python
    # Eviction mask update (thought eviction).
    UPDATE_MASK = b"\x06"
```

2. After the enum class, add:
```python
UPDATE_MASK_REQUEST_TYPE = EngineCoreRequestType.UPDATE_MASK
```

3. On `EngineCoreOutput`, append `new_l2_norms` as the LAST field (after `num_nans_in_logits`):
```python
    # Differential L2 norms from engine core (only new norms since last step).
    # None for non-eviction requests -- omitted from serialization by
    # omit_defaults=True so there is zero IPC overhead.
    new_l2_norms: list[float] | None = None
```

4. Remove the stale `eos_token_id` field from `EngineCoreRequest` (v0.19 removed it).
5. Restore `resumable` and `reasoning_ended` fields on `EngineCoreRequest`.
6. Restore `num_external_computed_tokens` on `EngineCoreOutput`.
7. Restore `PauseMode`, `EEPNotificationType`, `EEP_NOTIFICATION_CALL_ID`.
8. Restore `FinishReason.REPETITION = 4` and update `FINISH_REASON_STRINGS`.
9. Restore `ReconfigureDistributedRequest` fields.

**Step 2: Reconcile vllm/v1/core/sched/output.py (MERGE-07, per D-10)**

Strategy: Start from v0.19.0 version (`git show v0.19.0:vllm/v1/core/sched/output.py`), then add eviction field.

The current branch has these regressions vs v0.19:
- MISSING: `new_block_ids_to_zero: list[int] | None = None` (replaced by eviction field instead of alongside)
- PRESENT but stale: `@bc_linter_include` decorators (not in v0.19)
- PRESENT but stale: `from vllm._bc_linter import bc_linter_include`
- PRESENT but stale: `Optional` import from typing (v0.19 uses `| None`)

Write the v0.19.0 version of the file, then add the eviction field AFTER `new_block_ids_to_zero`:
```python
    # Block IDs freshly allocated... (v0.19 upstream)
    new_block_ids_to_zero: list[int] | None = None

    # Evictable token ranges for thought eviction (D-10).
    evictable_token_ranges_map: dict[str, list[tuple[int, int]]] | None = None
```

Remove `@bc_linter_include` decorators, `from vllm._bc_linter import bc_linter_include`, and `Optional` import.
  </action>
  <verify>
    <automated>cd /home/users/ntu/hong0259/vllm-thought-eviction && python -c "
from vllm.v1.engine import (
    EngineCoreRequestType, EngineCoreOutput, EngineCoreRequest,
    FinishReason, PauseMode, EEPNotificationType, EEP_NOTIFICATION_CALL_ID,
    FINISH_REASON_STRINGS, UPDATE_MASK_REQUEST_TYPE,
    ReconfigureDistributedRequest
)
# Verify byte values
assert EngineCoreRequestType.WAKEUP.value == b'\x05', 'WAKEUP wrong byte'
assert EngineCoreRequestType.UPDATE_MASK.value == b'\x06', 'UPDATE_MASK wrong byte'
assert FinishReason.REPETITION == 4, 'REPETITION missing'
assert 'repetition' in FINISH_REASON_STRINGS, 'repetition not in FINISH_REASON_STRINGS'
# Verify EngineCoreRequest fields
import msgspec
r_fields = {f.name for f in msgspec.structs.fields(EngineCoreRequest)}
assert 'resumable' in r_fields, 'resumable missing'
assert 'reasoning_ended' in r_fields, 'reasoning_ended missing'
assert 'eos_token_id' not in r_fields, 'stale eos_token_id present'
# Verify EngineCoreOutput has new_l2_norms and num_external_computed_tokens
o_fields = {f.name for f in msgspec.structs.fields(EngineCoreOutput)}
assert 'new_l2_norms' in o_fields, 'new_l2_norms missing on EngineCoreOutput'
assert 'num_external_computed_tokens' in o_fields, 'num_external_computed_tokens missing'
# Verify SchedulerOutput
from vllm.v1.core.sched.output import SchedulerOutput
import dataclasses
so_fields = {f.name for f in dataclasses.fields(SchedulerOutput)}
assert 'new_block_ids_to_zero' in so_fields, 'new_block_ids_to_zero missing'
assert 'evictable_token_ranges_map' in so_fields, 'evictable_token_ranges_map missing'
print('Task 2 verification passed')
" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `vllm/v1/engine/__init__.py` contains `WAKEUP = b"\x05"`
    - `vllm/v1/engine/__init__.py` contains `UPDATE_MASK = b"\x06"`
    - `vllm/v1/engine/__init__.py` contains `UPDATE_MASK_REQUEST_TYPE = EngineCoreRequestType.UPDATE_MASK`
    - `vllm/v1/engine/__init__.py` contains `PauseMode`
    - `vllm/v1/engine/__init__.py` contains `class EEPNotificationType`
    - `vllm/v1/engine/__init__.py` contains `REPETITION = 4`
    - `vllm/v1/engine/__init__.py` contains `"repetition"` in FINISH_REASON_STRINGS
    - `vllm/v1/engine/__init__.py` contains `new_l2_norms: list[float] | None = None` on EngineCoreOutput
    - `vllm/v1/engine/__init__.py` contains `resumable: bool = False` on EngineCoreRequest
    - `vllm/v1/engine/__init__.py` contains `reasoning_ended: bool | None = None` on EngineCoreRequest
    - `vllm/v1/engine/__init__.py` does NOT contain `eos_token_id` as a field on EngineCoreRequest
    - `vllm/v1/core/sched/output.py` contains `new_block_ids_to_zero: list[int] | None = None`
    - `vllm/v1/core/sched/output.py` contains `evictable_token_ranges_map: dict[str, list[tuple[int, int]]] | None = None`
    - `vllm/v1/core/sched/output.py` does NOT contain `bc_linter_include`
  </acceptance_criteria>
  <done>Engine __init__.py has all v0.19 features restored + UPDATE_MASK at b'\x06' + new_l2_norms on EngineCoreOutput; SchedulerOutput has both new_block_ids_to_zero and evictable_token_ranges_map</done>
</task>

<task type="auto">
  <name>Task 3: Verify eviction modules and tests are present and clean (MERGE-02, MERGE-03)</name>
  <files>
    vllm/v1/attention/l2_norm_cache.py,
    tests/eviction/test_strategies.py,
    tests/eviction/test_orchestrator.py,
    tests/eviction/test_segmenter.py,
    tests/eviction/test_block_utils.py,
    tests/eviction/test_smoke.py
  </files>
  <read_first>
    vllm/v1/attention/l2_norm_cache.py,
    vllm/thought_eviction/strategies.py,
    vllm/thought_eviction/block_utils.py,
    vllm/thought_eviction/__init__.py,
    tests/eviction/__init__.py
  </read_first>
  <action>
**Step 1: Verify l2_norm_cache.py (MERGE-02, per D-04)**

Read `vllm/v1/attention/l2_norm_cache.py` and confirm it is the Phase 1-cleaned version:
- Module-level singleton pattern (no `__new__`/`_instance`)
- No dead `update_norms()` method
- Has `get_l2_norm_cache()` function
- Uses `import regex as re` if regex is used (or no re import needed)

If any issues found, fix them. Otherwise no changes needed.

**Step 2: Verify remaining eviction modules are clean**

Read and verify these files have no `import re` violations:
- `vllm/thought_eviction/strategies.py` - should not use `re` at all
- `vllm/thought_eviction/block_utils.py` - should not use `re` at all
- `vllm/thought_eviction/__init__.py` - should be a clean package init

**Step 3: Verify test files (MERGE-03, per D-06, D-07)**

Read all test files in `tests/eviction/` and check:
- No `import re` violations (use `import regex as re` if regex needed)
- Imports reference correct module paths
- `test_smoke.py` is present (D-07)

If any test file has `import re`, fix to `import regex as re`.
If any test file imports from paths that changed in v0.19, update the import path.

Common import check: tests may import from `vllm.sampling_params` (verify `RepetitionDetectionParams` is available if referenced), `vllm.v1.engine` (verify enum values), `vllm.outputs` (verify `RequestOutput`).
  </action>
  <verify>
    <automated>cd /home/users/ntu/hong0259/vllm-thought-eviction && python -c "
# Verify l2_norm_cache
from vllm.v1.attention.l2_norm_cache import get_l2_norm_cache, L2NormCache
cache = get_l2_norm_cache()
assert isinstance(cache, L2NormCache)
# Verify strategies
from vllm.thought_eviction.strategies import GlobalStrategy, ThoughtMinStrategy, ThoughtAvgStrategy, RandomStrategy
# Verify block_utils
from vllm.thought_eviction.block_utils import merge_overlapping_ranges, align_ranges_to_blocks, apply_retention_window
# Verify test files exist
import os
test_dir = 'tests/eviction'
expected = ['test_strategies.py', 'test_orchestrator.py', 'test_segmenter.py', 'test_block_utils.py', 'test_smoke.py']
for f in expected:
    assert os.path.exists(os.path.join(test_dir, f)), f'Missing test: {f}'
print('Task 3 verification passed')
" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `vllm/v1/attention/l2_norm_cache.py` contains `def get_l2_norm_cache` and does NOT contain `def __new__`
    - `vllm/thought_eviction/strategies.py` does NOT contain bare `import re`
    - `vllm/thought_eviction/block_utils.py` does NOT contain bare `import re`
    - `tests/eviction/test_smoke.py` exists
    - `tests/eviction/test_strategies.py` exists
    - `tests/eviction/test_block_utils.py` exists
    - No file in `tests/eviction/` contains bare `import re` (without `regex`)
  </acceptance_criteria>
  <done>All eviction modules, l2_norm_cache, and test files verified clean and present; no import violations remain</done>
</task>

</tasks>

<verification>
1. `python -c "from vllm.thought_eviction import orchestrator, segmenter, strategies, block_utils"` succeeds
2. `python -c "from vllm.sampling_params import SamplingParams, RepetitionDetectionParams"` succeeds
3. `python -c "from vllm.v1.engine import EngineCoreRequestType; assert EngineCoreRequestType.WAKEUP.value == b'\\x05'; assert EngineCoreRequestType.UPDATE_MASK.value == b'\\x06'"` succeeds
4. `python -c "from vllm.v1.core.sched.output import SchedulerOutput; import dataclasses; f={f.name for f in dataclasses.fields(SchedulerOutput)}; assert 'new_block_ids_to_zero' in f and 'evictable_token_ranges_map' in f"` succeeds
5. `grep -r "^import re$" vllm/thought_eviction/ tests/eviction/` returns no matches
6. `git diff v0.19.0 -- vllm/sampling_params.py` shows only eviction field additions (no v0.19 features removed)
</verification>

<success_criteria>
- All v0.19 features restored in shared files (PauseMode, WAKEUP, RepetitionDetectionParams, new_block_ids_to_zero, STREAM_FINISHED, etc.)
- Eviction fields correctly positioned per D-08, D-09, D-10, D-11, D-12
- UPDATE_MASK at b"\x06" (no collision with WAKEUP)
- All eviction modules and tests import-clean
- Zero v0.14 stale fields remaining (logits_processors, truncate_prompt_tokens, eos_token_id on request)
</success_criteria>

<output>
After completion, create `.planning/phases/02-safe-additions/02-01-SUMMARY.md`
</output>

---
phase: 02.1-upstream-merge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - vllm/vllm_flash_attn/__init__.py
  - vllm/vllm_flash_attn/flash_attn_interface.py
  - vllm/v1/engine/__init__.py
  - vllm/v1/core/sched/output.py
  - vllm/v1/engine/async_llm.py
  - vllm/v1/engine/core.py
  - vllm/v1/engine/core_client.py
  - vllm/v1/worker/gpu_worker.py
  - vllm/sampling_params.py
  - vllm/envs.py
  - vllm/v1/engine/input_processor.py
  - vllm/entrypoints/openai/api_server.py
  - vllm/v1/core/sched/scheduler.py
  - vllm/v1/worker/gpu_model_runner.py
  - vllm/entrypoints/openai/chat_completion/serving.py
  - vllm/v1/attention/backends/flash_attn.py
autonomous: true
requirements: [UPSTREAM-01, UPSTREAM-02, UPSTREAM-03]

must_haves:
  truths:
    - "upstream/main is merged into upgrade_vllm with no unresolved conflict markers"
    - "All Pattern A files contain upstream's latest code PLUS eviction additions re-appended"
    - "All Pattern B files contain upstream's code with NO eviction code (deferred to Phase 3/4)"
    - "CLAUDE.md contains our project-specific version, not upstream's"
    - "The merge commit exists in git history"
  artifacts:
    - path: "vllm/v1/engine/__init__.py"
      provides: "UPDATE_MASK = b'\\x06' enum value after upstream's latest EngineCoreRequestType values"
      contains: "UPDATE_MASK"
    - path: "vllm/v1/core/sched/output.py"
      provides: "evictable_token_ranges_map on SchedulerOutput after all upstream fields"
      contains: "evictable_token_ranges_map"
    - path: "vllm/sampling_params.py"
      provides: "enable_l2_norms and l2_norm_layers fields after all upstream fields"
      contains: "enable_l2_norms"
    - path: "vllm/v1/engine/core.py"
      provides: "UPDATE_MASK dispatch and eviction utility methods"
      contains: "UPDATE_MASK"
    - path: "vllm/v1/engine/async_llm.py"
      provides: "update_request_mask and get_request_l2_norms methods"
      contains: "update_request_mask"
    - path: "vllm/v1/worker/gpu_worker.py"
      provides: "Eviction method implementations"
      contains: "get_request_l2_norms"
  key_links:
    - from: "vllm/v1/engine/__init__.py"
      to: "vllm/v1/engine/core.py"
      via: "UPDATE_MASK enum used in dispatch"
      pattern: "UPDATE_MASK"
    - from: "vllm/v1/core/sched/output.py"
      to: "vllm/v1/worker/gpu_model_runner.py"
      via: "SchedulerOutput.evictable_token_ranges_map consumed by runner"
      pattern: "evictable_token_ranges_map"
---

<objective>
Merge upstream/main into upgrade_vllm branch, resolving all 14 file conflicts using the two-pattern strategy from research: Pattern A (accept upstream + re-append eviction) for 10 files, Pattern B (accept upstream wholesale) for 4 files.

Purpose: Bring upgrade_vllm branch up to date with upstream vLLM, preserving Phase 2's eviction additions while accepting all upstream changes. This unblocks Phase 3 (core adaptation) which needs the latest upstream APIs.

Output: A single merge commit with all conflicts resolved, eviction code preserved on Pattern A files, and Pattern B files ready for Phase 3/4 re-application.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-upstream-merge-merge-upstream-main-with-eviction-adaptation-during-conflict-resolution/02.1-RESEARCH.md
@.planning/phases/02-safe-additions/02-01-SUMMARY.md
@.planning/phases/02-safe-additions/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-merge setup, execute merge, resolve Pattern B conflicts</name>
  <files>
    CLAUDE.md,
    vllm/vllm_flash_attn/__init__.py,
    vllm/vllm_flash_attn/flash_attn_interface.py,
    vllm/v1/core/sched/scheduler.py,
    vllm/v1/worker/gpu_model_runner.py,
    vllm/entrypoints/openai/chat_completion/serving.py,
    vllm/v1/attention/backends/flash_attn.py
  </files>
  <read_first>
    .planning/phases/02.1-upstream-merge-merge-upstream-main-with-eviction-adaptation-during-conflict-resolution/02.1-RESEARCH.md
  </read_first>
  <action>
**Step 1: Record pre-merge reference commit**

```bash
PRE_MERGE_REF=$(git rev-parse HEAD)
echo "Pre-merge ref: $PRE_MERGE_REF"
```

Save this value -- Phase 3/4 plans will use `git show $PRE_MERGE_REF:path/to/file` to reference eviction code removed by Pattern B resolution.

**Step 2: Move gitignored files that block merge**

Three files exist locally (gitignored) but are tracked by upstream. Git will refuse to merge unless they are moved:

```bash
mv CLAUDE.md CLAUDE.md.bak
mv vllm/vllm_flash_attn/__init__.py /tmp/flash_init.bak
mv vllm/vllm_flash_attn/flash_attn_interface.py /tmp/flash_iface.bak
```

**Step 3: Execute the merge**

```bash
git merge upstream/main --no-edit
```

This will report 14 conflicting files and leave the repo in a merge-in-progress state. Do NOT abort.

**Step 4: Resolve Pattern B files (accept upstream wholesale)**

These 4 files contain eviction logic that belongs to Phase 3/4. Accept upstream's version completely:

```bash
git checkout upstream/main -- vllm/v1/core/sched/scheduler.py
git checkout upstream/main -- vllm/v1/worker/gpu_model_runner.py
git checkout upstream/main -- vllm/entrypoints/openai/chat_completion/serving.py
git checkout upstream/main -- vllm/v1/attention/backends/flash_attn.py
```

After each checkout, `git add` the file to mark it resolved.

**Step 5: Restore CLAUDE.md**

```bash
mv CLAUDE.md.bak CLAUDE.md
```

Keep our project-specific CLAUDE.md, NOT upstream's version. The upstream CLAUDE.md (if created by the merge) should be replaced with our backup.

For flash_attn files: keep upstream's tracked versions (they are more current than our gitignored copies). Do NOT restore the backups from /tmp.

**IMPORTANT:** Do NOT commit yet. Pattern A files still have unresolved conflicts. Leave the merge in progress for Task 2.
  </action>
  <verify>
    <automated>
# Verify Pattern B files have no conflict markers
grep -c "<<<<<<" vllm/v1/core/sched/scheduler.py vllm/v1/worker/gpu_model_runner.py vllm/entrypoints/openai/chat_completion/serving.py vllm/v1/attention/backends/flash_attn.py 2>/dev/null | grep -v ":0$" | wc -l
# Should output 0 (no files with markers)
    </automated>
  </verify>
  <acceptance_criteria>
    - `git merge upstream/main --no-edit` executed (merge in progress)
    - `vllm/v1/core/sched/scheduler.py` contains NO `<<<<<<` conflict markers and matches upstream/main
    - `vllm/v1/worker/gpu_model_runner.py` contains NO `<<<<<<` conflict markers and matches upstream/main
    - `vllm/entrypoints/openai/chat_completion/serving.py` contains NO `<<<<<<` conflict markers and matches upstream/main
    - `vllm/v1/attention/backends/flash_attn.py` contains NO `<<<<<<` conflict markers and matches upstream/main
    - `CLAUDE.md` contains `vLLM Thought Eviction` (our version, not upstream's)
    - None of the Pattern B files contain eviction-specific strings like `evict_kv_blocks`, `_compute_l2_norms`, `eviction_params` (those belong to Phase 3/4)
  </acceptance_criteria>
  <done>Pattern B files resolved to upstream, gitignored files handled, merge in progress ready for Pattern A resolution</done>
</task>

<task type="auto">
  <name>Task 2: Resolve Pattern A conflicts — re-append eviction code onto upstream base</name>
  <files>
    vllm/v1/engine/__init__.py,
    vllm/v1/core/sched/output.py,
    vllm/v1/engine/async_llm.py,
    vllm/v1/engine/core.py,
    vllm/v1/engine/core_client.py,
    vllm/v1/worker/gpu_worker.py,
    vllm/sampling_params.py,
    vllm/envs.py,
    vllm/v1/engine/input_processor.py,
    vllm/entrypoints/openai/api_server.py
  </files>
  <read_first>
    .planning/phases/02.1-upstream-merge-merge-upstream-main-with-eviction-adaptation-during-conflict-resolution/02.1-RESEARCH.md,
    .planning/phases/02-safe-additions/02-01-SUMMARY.md,
    .planning/phases/02-safe-additions/02-02-SUMMARY.md
  </read_first>
  <action>
For each of the 10 Pattern A files: open the file, find conflict markers (`<<<<<<<`, `=======`, `>>>>>>>`), and resolve by keeping upstream's code as the base, then re-appending eviction additions. Use `git show HEAD:path/to/file` (pre-merge version) to see the eviction code that needs re-appending.

**Resolution procedure for each file:**

1. Read the conflicted file to understand each conflict region
2. Read `git show MERGE_HEAD:path/to/file` for upstream's version
3. Read `git show HEAD:path/to/file` for our pre-merge version (shows what eviction code to re-append)
4. For each conflict region: accept upstream's code, then add back eviction-specific lines from our version
5. Remove ALL conflict markers (`<<<<<<<`, `=======`, `>>>>>>>`)
6. `git add` the file

**File-by-file resolution details:**

**1. `vllm/v1/engine/__init__.py` (1 conflict)**
- Re-append after upstream's last `EngineCoreRequestType` value: `UPDATE_MASK = b"\x06"`
- Re-append the alias: `UPDATE_MASK_REQUEST_TYPE = EngineCoreRequestType.UPDATE_MASK`
- Ensure `new_l2_norms` is the LAST field on `EngineCoreOutput` (after ALL upstream fields including any newly added ones)

**2. `vllm/v1/core/sched/output.py` (1 conflict)**
- Re-append `evictable_token_ranges_map: dict[str, list[tuple[int, int]]] = msgspec.field(default_factory=dict)` as the last field on `SchedulerOutput`

**3. `vllm/v1/engine/async_llm.py` (1 conflict)**
- Re-append `update_request_mask` and `get_request_l2_norms` methods at the end of the `AsyncLLM` class

**4. `vllm/v1/engine/core.py` (2 conflicts)**
- Re-append `update_request_mask`, `get_request_l2_norms`, `configure_l2_norms` methods on `EngineCore`
- Re-append `UPDATE_MASK` case in `_handle_client_request` dispatch (after ABORT, before UTILITY)

**5. `vllm/v1/engine/core_client.py` (1 conflict)**
- Re-append eviction IPC methods on all client classes (InprocClient, SyncMPClient, AsyncMPClient). This was a trivial whitespace conflict per research.
- DPAsyncMPClient and DPLBAsyncMPClient inherit from AsyncMPClient -- no explicit overrides needed.

**6. `vllm/v1/worker/gpu_worker.py` (2 conflicts)**
- Re-append `get_request_l2_norms`, `configure_l2_norms`, `evict_kv_blocks` methods
- Accept upstream's `get_compilation_match_table` method (new in upstream)

**7. `vllm/sampling_params.py` (5 conflicts)**
- Re-append `enable_l2_norms: bool = False` and `l2_norm_layers: Optional[list[int]] = None` as the LAST fields in `SamplingParams` (after ALL upstream fields including any new Mistral tokenizer validation)
- Accept ALL of upstream's new validation logic, fields, and imports

**8. `vllm/envs.py` (2 conflicts)**
- Re-append `VLLM_KV_REPLACEMENT_STRATEGY: str = ...` env var definition
- Accept ALL of upstream's new env vars

**9. `vllm/v1/engine/input_processor.py` (1 conflict)**
- Accept upstream's `VLLM_DISABLE_REQUEST_ID_RANDOMIZATION` env var approach
- Do NOT keep our hardcoded UUID suffix removal -- upstream's env var approach is better
- Eviction tests will set the env var if needed

**10. `vllm/entrypoints/openai/api_server.py` (1 conflict)**
- Re-append attention tools router registration (the route that registers `/v1/attention/l2_norms`)

**CRITICAL ORDERING RULES (msgspec):**
- `new_l2_norms` MUST be the absolute LAST field in `EngineCoreOutput`
- `enable_l2_norms` and `l2_norm_layers` MUST be the LAST fields in `SamplingParams`
- `evictable_token_ranges_map` MUST be the LAST field in `SchedulerOutput`
- Field ordering is byte-significant for `array_like=True, omit_defaults=True` serialization

After resolving ALL 10 files, run `git add` on each.
  </action>
  <verify>
    <automated>
# No conflict markers in ANY file
grep -r "<<<<<<" vllm/ tests/ --include="*.py" | head -5
# Should produce no output

# Eviction fields present in Pattern A files
grep -q "UPDATE_MASK" vllm/v1/engine/__init__.py && echo "OK: UPDATE_MASK" || echo "FAIL: UPDATE_MASK"
grep -q "evictable_token_ranges_map" vllm/v1/core/sched/output.py && echo "OK: sched_output" || echo "FAIL: sched_output"
grep -q "update_request_mask" vllm/v1/engine/async_llm.py && echo "OK: async_llm" || echo "FAIL: async_llm"
grep -q "UPDATE_MASK" vllm/v1/engine/core.py && echo "OK: core" || echo "FAIL: core"
grep -q "enable_l2_norms" vllm/sampling_params.py && echo "OK: sampling_params" || echo "FAIL: sampling_params"
grep -q "VLLM_KV_REPLACEMENT_STRATEGY" vllm/envs.py && echo "OK: envs" || echo "FAIL: envs"
grep -q "get_request_l2_norms" vllm/v1/worker/gpu_worker.py && echo "OK: gpu_worker" || echo "FAIL: gpu_worker"
    </automated>
  </verify>
  <acceptance_criteria>
    - Zero files contain `<<<<<<` conflict markers across entire repo
    - `vllm/v1/engine/__init__.py` contains `UPDATE_MASK = b"\\x06"` and `new_l2_norms` is the last field in EngineCoreOutput
    - `vllm/v1/core/sched/output.py` contains `evictable_token_ranges_map` as last field in SchedulerOutput
    - `vllm/v1/engine/async_llm.py` contains `update_request_mask` and `get_request_l2_norms`
    - `vllm/v1/engine/core.py` contains `UPDATE_MASK` dispatch and `update_request_mask`, `get_request_l2_norms`, `configure_l2_norms` methods
    - `vllm/v1/engine/core_client.py` contains eviction IPC methods on client classes
    - `vllm/v1/worker/gpu_worker.py` contains `get_request_l2_norms`, `configure_l2_norms`, `evict_kv_blocks`
    - `vllm/sampling_params.py` contains `enable_l2_norms` and `l2_norm_layers` as LAST fields
    - `vllm/envs.py` contains `VLLM_KV_REPLACEMENT_STRATEGY`
    - `vllm/v1/engine/input_processor.py` uses upstream's `VLLM_DISABLE_REQUEST_ID_RANDOMIZATION` (NOT our hardcoded removal)
    - `vllm/entrypoints/openai/api_server.py` registers attention tools router
  </acceptance_criteria>
  <done>All 10 Pattern A conflicts resolved with upstream base + eviction re-appended, all 14 total conflicts resolved, repo ready for merge commit</done>
</task>

<task type="auto">
  <name>Task 3: Complete merge commit and record pre-merge reference</name>
  <files>
    .planning/phases/02.1-upstream-merge-merge-upstream-main-with-eviction-adaptation-during-conflict-resolution/PRE_MERGE_REF.txt
  </files>
  <read_first>
    .planning/phases/02.1-upstream-merge-merge-upstream-main-with-eviction-adaptation-during-conflict-resolution/02.1-RESEARCH.md
  </read_first>
  <action>
**Step 1: Final pre-commit verification**

Run these checks before committing:

```bash
# No conflict markers anywhere
grep -r "<<<<<<" vllm/ tests/ --include="*.py" | head -5

# Eviction modules still exist
ls vllm/thought_eviction/orchestrator.py vllm/thought_eviction/segmenter.py vllm/thought_eviction/strategies.py vllm/thought_eviction/block_utils.py

# Eviction test files still exist
ls tests/eviction/

# Key eviction fields grep check
grep -c "UPDATE_MASK" vllm/v1/engine/__init__.py
grep -c "new_l2_norms" vllm/v1/engine/__init__.py
grep -c "evictable_token_ranges_map" vllm/v1/core/sched/output.py
grep -c "enable_l2_norms" vllm/sampling_params.py
```

**Step 2: Stage and commit the merge**

```bash
git add -A
git commit --no-edit
```

The merge commit message is auto-generated by git ("Merge remote-tracking branch 'upstream/main' into upgrade_vllm").

**Step 3: Save pre-merge reference for Phase 3/4**

Write the pre-merge commit hash (the parent of the merge commit that was HEAD before merge) to a reference file so Phase 3/4 executors can find eviction code that was removed from Pattern B files:

The pre-merge ref is the first parent of the merge commit: `git rev-parse HEAD^1`

Create file `.planning/phases/02.1-upstream-merge-merge-upstream-main-with-eviction-adaptation-during-conflict-resolution/PRE_MERGE_REF.txt` containing:

```
# Pre-merge reference commit
# Use: git show <hash>:path/to/file to see eviction code removed by Pattern B resolution
# Needed by: Phase 3 (scheduler, gpu_model_runner), Phase 4 (serving)
<hash>
```
  </action>
  <verify>
    <automated>
# Merge commit exists
git log --oneline -1 | grep -i "merge" && echo "OK: merge commit" || echo "FAIL: no merge commit"

# No conflict markers
grep -r "<<<<<<" vllm/ tests/ --include="*.py" | wc -l
# Should output 0

# Eviction modules importable path check
ls vllm/thought_eviction/__init__.py vllm/thought_eviction/orchestrator.py vllm/thought_eviction/segmenter.py vllm/thought_eviction/strategies.py vllm/thought_eviction/block_utils.py && echo "OK: eviction modules" || echo "FAIL: eviction modules"

# Pre-merge ref file exists
test -f .planning/phases/02.1-upstream-merge-merge-upstream-main-with-eviction-adaptation-during-conflict-resolution/PRE_MERGE_REF.txt && echo "OK: ref file" || echo "FAIL: ref file"
    </automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1` shows a merge commit (contains "Merge")
    - `grep -r "<<<<<<" vllm/ tests/ --include="*.py"` produces no output
    - All files in `vllm/thought_eviction/` exist: `__init__.py`, `orchestrator.py`, `segmenter.py`, `strategies.py`, `block_utils.py`
    - All files in `tests/eviction/` exist
    - `PRE_MERGE_REF.txt` exists and contains a valid git hash
    - `git show $(cat .planning/phases/02.1-upstream-merge-merge-upstream-main-with-eviction-adaptation-during-conflict-resolution/PRE_MERGE_REF.txt | tail -1):vllm/v1/core/sched/scheduler.py` succeeds (proves the ref is valid for Phase 3 code lookup)
  </acceptance_criteria>
  <done>Merge commit created, pre-merge reference saved, repo in clean state with upstream/main merged</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `git log --oneline -1` shows merge commit
2. `grep -r "<<<<<<" vllm/ tests/ --include="*.py"` returns nothing
3. All eviction modules exist in `vllm/thought_eviction/`
4. All eviction test files exist in `tests/eviction/`
5. Pattern A eviction fields present: `UPDATE_MASK`, `new_l2_norms`, `evictable_token_ranges_map`, `enable_l2_norms`, `VLLM_KV_REPLACEMENT_STRATEGY`
6. Pattern B files contain NO eviction code (clean upstream)
7. Pre-merge reference commit is recorded for Phase 3/4
</verification>

<success_criteria>
- upstream/main fully merged into upgrade_vllm branch
- Zero conflict markers in codebase
- All Phase 2 eviction additions preserved in Pattern A files
- Pattern B files clean for Phase 3/4 re-application
- Pre-merge reference available for eviction code lookup
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-upstream-merge-merge-upstream-main-with-eviction-adaptation-during-conflict-resolution/02.1-01-SUMMARY.md`
</output>

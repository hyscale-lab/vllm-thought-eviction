---
phase: 02.1-upstream-merge
plan: 02
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - vllm/entrypoints/openai/chat_completion/protocol.py
  - vllm/v1/engine/output_processor.py
  - vllm/outputs.py
  - vllm/v1/core/kv_cache_coordinator.py
  - vllm/v1/core/kv_cache_manager.py
  - vllm/v1/core/single_type_kv_cache_manager.py
  - vllm/v1/worker/worker_base.py
  - vllm/v1/engine/__init__.py
  - vllm/v1/core/sched/output.py
  - vllm/sampling_params.py
autonomous: true
requirements: [UPSTREAM-04, UPSTREAM-05]

must_haves:
  truths:
    - "Auto-merged files preserved eviction fields correctly"
    - "msgspec field ordering is correct (eviction fields last) in all structs"
    - "Eviction modules import without errors"
    - "No eviction code was silently dropped by auto-merge"
  artifacts:
    - path: "vllm/entrypoints/openai/chat_completion/protocol.py"
      provides: "EvictionParams model and eviction_params field on ChatCompletionRequest"
      contains: "EvictionParams"
    - path: "vllm/v1/engine/output_processor.py"
      provides: "new_l2_norms propagation from EngineCoreOutput to RequestOutput"
      contains: "new_l2_norms"
    - path: "vllm/outputs.py"
      provides: "new_l2_norms field on RequestOutput"
      contains: "new_l2_norms"
    - path: "vllm/v1/worker/worker_base.py"
      provides: "Eviction ABCs on WorkerBase"
      contains: "get_request_l2_norms"
  key_links:
    - from: "vllm/v1/engine/output_processor.py"
      to: "vllm/outputs.py"
      via: "new_l2_norms propagated from EngineCoreOutput to RequestOutput"
      pattern: "new_l2_norms"
    - from: "vllm/entrypoints/openai/chat_completion/protocol.py"
      to: "vllm/thought_eviction/orchestrator.py"
      via: "EvictionParams consumed by EvictionOrchestrator"
      pattern: "EvictionParams"
---

<objective>
Verify all 7 auto-merged files preserved eviction code correctly, and validate msgspec field ordering in key structs. Fix any issues found.

Purpose: Git auto-merge can silently corrupt eviction additions by inserting upstream fields after our eviction fields (breaking msgspec byte ordering) or by dropping eviction code during complex three-way merges. This plan catches those failures.

Output: Verified (and if needed, fixed) auto-merged files with correct eviction code positioning.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-upstream-merge-merge-upstream-main-with-eviction-adaptation-during-conflict-resolution/02.1-RESEARCH.md
@.planning/phases/02-safe-additions/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify auto-merged eviction fields and fix msgspec ordering</name>
  <files>
    vllm/entrypoints/openai/chat_completion/protocol.py,
    vllm/v1/engine/output_processor.py,
    vllm/outputs.py,
    vllm/v1/core/kv_cache_coordinator.py,
    vllm/v1/core/kv_cache_manager.py,
    vllm/v1/core/single_type_kv_cache_manager.py,
    vllm/v1/worker/worker_base.py
  </files>
  <read_first>
    vllm/entrypoints/openai/chat_completion/protocol.py,
    vllm/v1/engine/output_processor.py,
    vllm/outputs.py,
    vllm/v1/core/kv_cache_coordinator.py,
    vllm/v1/core/kv_cache_manager.py,
    vllm/v1/core/single_type_kv_cache_manager.py,
    vllm/v1/worker/worker_base.py
  </read_first>
  <action>
Verify each of the 7 auto-merged files. For each file, confirm the eviction-specific code is present and correctly positioned. Fix any issues found.

**1. `vllm/entrypoints/openai/chat_completion/protocol.py` (MEDIUM risk)**
- Verify `EvictionParams` class exists (Pydantic model with strategy, keep_ratio, etc.)
- Verify `eviction_params: Optional[EvictionParams] = None` field exists on `ChatCompletionRequest`
- Verify the Pydantic model is intact (no syntax errors from merge)

**2. `vllm/v1/engine/output_processor.py` (MEDIUM risk)**
- Verify `new_l2_norms` is propagated from `EngineCoreOutput` to `RequestOutput`
- Look for a line like `new_l2_norms=engine_core_output.new_l2_norms` or equivalent assignment
- If upstream refactored the output processor, the propagation may need re-wiring

**3. `vllm/outputs.py` (LOW risk)**
- Verify `new_l2_norms` field exists on `RequestOutput`
- Should be: `new_l2_norms: Optional[list[float]] = None` or similar

**4. `vllm/v1/core/kv_cache_coordinator.py` (LOW risk)**
- Verify upstream structural changes don't break the eviction `free_blocks` path
- Look for `free_blocks` method or equivalent -- ensure it accepts the block indices pattern eviction needs

**5. `vllm/v1/core/kv_cache_manager.py` (LOW risk)**
- Same as coordinator -- verify `free_blocks` path exists

**6. `vllm/v1/core/single_type_kv_cache_manager.py` (LOW risk)**
- Same -- verify block freeing capability exists

**7. `vllm/v1/worker/worker_base.py` (LOW risk)**
- Verify eviction ABCs exist: `get_request_l2_norms`, `configure_l2_norms` as abstract methods on `WorkerBase`

**For each file:** If eviction code is missing or mispositioned, fix it by adding back the eviction code from the pre-merge reference (use `git show <PRE_MERGE_REF>:path/to/file` where PRE_MERGE_REF is in `.planning/phases/02.1-upstream-merge-merge-upstream-main-with-eviction-adaptation-during-conflict-resolution/PRE_MERGE_REF.txt`).
  </action>
  <verify>
    <automated>
# Eviction fields in auto-merged files
grep -q "EvictionParams" vllm/entrypoints/openai/chat_completion/protocol.py && echo "OK: protocol" || echo "FAIL: protocol"
grep -q "eviction_params" vllm/entrypoints/openai/chat_completion/protocol.py && echo "OK: eviction_params field" || echo "FAIL: eviction_params field"
grep -q "new_l2_norms" vllm/v1/engine/output_processor.py && echo "OK: output_processor" || echo "FAIL: output_processor"
grep -q "new_l2_norms" vllm/outputs.py && echo "OK: outputs" || echo "FAIL: outputs"
grep -q "get_request_l2_norms" vllm/v1/worker/worker_base.py && echo "OK: worker_base" || echo "FAIL: worker_base"
    </automated>
  </verify>
  <acceptance_criteria>
    - `vllm/entrypoints/openai/chat_completion/protocol.py` contains `class EvictionParams` and `eviction_params`
    - `vllm/v1/engine/output_processor.py` contains `new_l2_norms` (propagation logic)
    - `vllm/outputs.py` contains `new_l2_norms` field on RequestOutput
    - `vllm/v1/worker/worker_base.py` contains `get_request_l2_norms` and `configure_l2_norms`
    - `vllm/v1/core/kv_cache_coordinator.py` has block-freeing capability (method accepting block indices)
    - No conflict markers (`<<<<<<`) in any of the 7 files
  </acceptance_criteria>
  <done>All 7 auto-merged files verified to contain correct eviction code</done>
</task>

<task type="auto">
  <name>Task 2: Validate msgspec field ordering in key structs</name>
  <files>
    vllm/v1/engine/__init__.py,
    vllm/v1/core/sched/output.py,
    vllm/sampling_params.py
  </files>
  <read_first>
    vllm/v1/engine/__init__.py,
    vllm/v1/core/sched/output.py,
    vllm/sampling_params.py
  </read_first>
  <action>
Validate that eviction fields are in the CORRECT position in all three msgspec-sensitive structs. This is the most critical post-merge check -- wrong ordering causes silent data corruption in IPC.

**1. `vllm/v1/engine/__init__.py` — `EngineCoreOutput`**

Read the `EngineCoreOutput` class definition. Verify:
- `new_l2_norms` is the ABSOLUTE LAST field (after `num_nans_in_logits`, `routed_experts`, and any new upstream fields)
- The class uses `msgspec.Struct` with `array_like=True`
- No upstream field appears AFTER `new_l2_norms`

If `new_l2_norms` is NOT last: move it to the last position. The field should be:
```python
new_l2_norms: Optional[list[float]] = None
```

**2. `vllm/v1/core/sched/output.py` — `SchedulerOutput`**

Read the `SchedulerOutput` class definition. Verify:
- `evictable_token_ranges_map` is the LAST field
- No upstream field appears after it

If not last: move it to last position. The field should be:
```python
evictable_token_ranges_map: dict[str, list[tuple[int, int]]] = msgspec.field(default_factory=dict)
```

**3. `vllm/sampling_params.py` — `SamplingParams`**

Read the class definition. Verify:
- `enable_l2_norms` and `l2_norm_layers` are the LAST regular fields (before any `__post_init__` or private fields)
- No upstream field appears after them

If not last: move them to last position. The fields should be:
```python
enable_l2_norms: bool = False
l2_norm_layers: Optional[list[int]] = None
```

**If any fixes are needed:** Make the fix, then commit with message `fix(02.1): correct msgspec field ordering after auto-merge`.
  </action>
  <verify>
    <automated>
# Check new_l2_norms is last field in EngineCoreOutput
# Get the last field definition line before the class ends
python3 -c "
import ast, sys
with open('vllm/v1/engine/__init__.py') as f:
    content = f.read()
# Find EngineCoreOutput class and check new_l2_norms position
idx = content.find('class EngineCoreOutput')
if idx == -1:
    print('FAIL: EngineCoreOutput not found')
    sys.exit(1)
# Find the class body end (next class or end of indented block)
lines = content[idx:].split('\n')
fields = []
for line in lines[1:]:
    stripped = line.strip()
    if stripped and not stripped.startswith('#') and not stripped.startswith('def ') and ':' in stripped and not stripped.startswith('class '):
        if '=' in stripped or ':' in stripped:
            name = stripped.split(':')[0].strip()
            if name and not name.startswith('_'):
                fields.append(name)
    if stripped.startswith('class ') or (stripped and not line.startswith(' ') and not line.startswith('\t') and lines.index(line) > 0):
        break
if fields and fields[-1] == 'new_l2_norms':
    print('OK: new_l2_norms is last field in EngineCoreOutput')
else:
    print(f'FAIL: last field is {fields[-1] if fields else \"none\"}, expected new_l2_norms')
    print(f'Fields found: {fields}')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - In `EngineCoreOutput`: `new_l2_norms` is the last field (no field definition line after it before class end or next method)
    - In `SchedulerOutput`: `evictable_token_ranges_map` is the last field
    - In `SamplingParams`: `enable_l2_norms` and `l2_norm_layers` are the last regular fields (before `__post_init__` if present)
    - No conflict markers in any of the 3 files
  </acceptance_criteria>
  <done>msgspec field ordering verified correct (or fixed) — eviction IPC serialization will work</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. All 7 auto-merged files contain correct eviction code
2. All 3 msgspec-sensitive structs have eviction fields in LAST position
3. No conflict markers anywhere in codebase
4. Eviction modules exist and tests exist
5. Pre-merge reference is available for Phase 3/4
</verification>

<success_criteria>
- Auto-merged eviction code verified intact in all 7 files
- msgspec field ordering confirmed correct in EngineCoreOutput, SchedulerOutput, SamplingParams
- Any auto-merge corruption detected and fixed
- Codebase ready for Phase 3 core adaptation
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-upstream-merge-merge-upstream-main-with-eviction-adaptation-during-conflict-resolution/02.1-02-SUMMARY.md`
</output>

---
phase: 02.1-upstream-merge
plan: 01
subsystem: git-merge
tags: [upstream, merge, conflict-resolution, eviction-preservation, v0.19]

requires:
  - phase: 02-safe-additions
    plan: 01
    provides: "Reconciled struct files (sampling_params, outputs, engine __init__, sched/output, l2_norm_cache)"
  - phase: 02-safe-additions
    plan: 02
    provides: "Reconciled IPC pipeline files (worker_base, gpu_worker, core_client, async_llm, core)"
provides:
  - "upstream/main merged into upgrade_vllm with all 14 conflicts resolved"
  - "Pattern A files: upstream base + eviction code re-appended (10 files)"
  - "Pattern B files: clean upstream for Phase 3/4 re-application (4 files)"
  - "Pre-merge reference commit for Phase 3/4 eviction code lookup"
affects: [03-core-adaptation, 04-serving, scheduler, gpu-model-runner, serving]

tech-stack:
  added: []
  patterns: ["Two-pattern merge resolution: Pattern A (upstream + eviction) vs Pattern B (upstream wholesale)"]

key-files:
  created:
    - .planning/phases/02.1-upstream-merge-merge-upstream-main-with-eviction-adaptation-during-conflict-resolution/PRE_MERGE_REF.txt
  modified:
    - vllm/v1/engine/__init__.py
    - vllm/v1/core/sched/output.py
    - vllm/v1/engine/async_llm.py
    - vllm/v1/engine/core.py
    - vllm/v1/engine/core_client.py
    - vllm/v1/worker/gpu_worker.py
    - vllm/sampling_params.py
    - vllm/envs.py
    - vllm/v1/engine/input_processor.py
    - vllm/entrypoints/openai/api_server.py
    - vllm/v1/core/sched/scheduler.py
    - vllm/v1/worker/gpu_model_runner.py
    - vllm/entrypoints/openai/chat_completion/serving.py
    - vllm/v1/attention/backends/flash_attn.py

key-decisions:
  - "Pattern B files (scheduler, gpu_model_runner, serving, flash_attn) accepted upstream wholesale; eviction code deferred to Phase 3/4"
  - "Adopted upstream VLLM_DISABLE_REQUEST_ID_RANDOMIZATION env var instead of hardcoded UUID removal in input_processor"
  - "Accepted upstream _is_non_tekken_mistral validation replacing old is_mistral_tokenizer checks in sampling_params"
  - "Kept upstream get_compilation_match_table method on GPUWorker (new in upstream)"

patterns-established:
  - "Pre-merge ref pattern: save commit hash for Phase 3/4 to reference removed eviction code via git show"

requirements-completed: [UPSTREAM-01, UPSTREAM-02, UPSTREAM-03]

duration: 8min
completed: 2026-04-08
---

# Phase 02.1 Plan 01: Upstream Merge Summary

**Merged 2527 upstream/main commits into upgrade_vllm resolving all 14 file conflicts: 10 files with eviction code re-appended, 4 files accepted upstream wholesale for Phase 3/4**

## Performance

- **Duration:** 8 min
- **Started:** 2026-04-08T12:01:47Z
- **Completed:** 2026-04-08T12:10:15Z
- **Tasks:** 3
- **Files modified:** 14 (conflict resolution) + thousands auto-merged

## Accomplishments
- Merged upstream/main (2527 commits) into upgrade_vllm with zero remaining conflict markers
- All 10 Pattern A files retain eviction code (UPDATE_MASK, new_l2_norms, evictable_token_ranges_map, enable_l2_norms, VLLM_KV_REPLACEMENT_STRATEGY, attention router, eviction IPC methods)
- All 4 Pattern B files contain clean upstream code ready for Phase 3/4 eviction re-application
- Pre-merge reference commit (2ec9a65e84) saved for Phase 3/4 to look up removed eviction code
- Auto-merged files verified: protocol.py, output_processor.py, outputs.py, worker_base.py all retain eviction fields

## Task Commits

Tasks 1-3 are part of a single merge operation:

1. **Tasks 1-3: Merge upstream/main, resolve all 14 conflicts** - `5f1db66ee9` (merge)
2. **Pre-merge reference doc** - `319c503c21` (docs)

## Files Created/Modified

**Pattern A (upstream + eviction re-appended):**
- `vllm/v1/engine/__init__.py` - UPDATE_MASK = b"\x06" enum + new_l2_norms last field on EngineCoreOutput
- `vllm/v1/core/sched/output.py` - evictable_token_ranges_map last field on SchedulerOutput
- `vllm/v1/engine/async_llm.py` - update_request_mask, evict_kv_blocks, get_request_l2_norms methods
- `vllm/v1/engine/core.py` - Eviction methods + UPDATE_MASK dispatch in _handle_client_request
- `vllm/v1/engine/core_client.py` - Trivial whitespace conflict resolved
- `vllm/v1/worker/gpu_worker.py` - Eviction methods + upstream get_compilation_match_table
- `vllm/sampling_params.py` - enable_l2_norms + l2_norm_layers + upstream _is_non_tekken_mistral
- `vllm/envs.py` - VLLM_KV_REPLACEMENT_STRATEGY + upstream's 11 new env vars
- `vllm/v1/engine/input_processor.py` - Upstream VLLM_DISABLE_REQUEST_ID_RANDOMIZATION env var
- `vllm/entrypoints/openai/api_server.py` - Attention tools router registration

**Pattern B (accepted upstream wholesale):**
- `vllm/v1/core/sched/scheduler.py` - Clean upstream, eviction deferred to Phase 3
- `vllm/v1/worker/gpu_model_runner.py` - Clean upstream, eviction deferred to Phase 3
- `vllm/entrypoints/openai/chat_completion/serving.py` - Clean upstream, eviction deferred to Phase 4
- `vllm/v1/attention/backends/flash_attn.py` - Clean upstream, eviction deferred to Phase 3

## Decisions Made
- Pattern B files accepted upstream wholesale; eviction code for these files belongs to Phase 3/4
- Adopted upstream's `VLLM_DISABLE_REQUEST_ID_RANDOMIZATION` env var approach instead of our hardcoded UUID suffix removal
- Accepted upstream's `_is_non_tekken_mistral` validation replacing older `is_mistral_tokenizer` checks
- Kept upstream's new `get_compilation_match_table` method on GPUWorker

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs
None - all eviction code is real implementation. Pattern B files intentionally contain no eviction code (deferred to Phase 3/4).

## Issues Encountered
None - merge proceeded as expected with all 14 conflicts matching research predictions.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Upstream merged, codebase up to date with vLLM v0.19.0
- Phase 3 can reference pre-merge commit (2ec9a65e84) via `git show 2ec9a65e84:path/to/file` for eviction code
- Pattern B files (scheduler, gpu_model_runner, serving, flash_attn) ready for Phase 3/4 eviction re-application
- All eviction modules in `vllm/thought_eviction/` intact
- All eviction tests in `tests/eviction/` intact

---
*Phase: 02.1-upstream-merge*
*Completed: 2026-04-08*

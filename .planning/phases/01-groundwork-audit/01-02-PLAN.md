---
phase: 01-groundwork-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-groundwork-audit/01-AUDIT-FINDINGS.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04
  - AUDIT-05

must_haves:
  truths:
    - "EngineCoreRequestType byte values in v0.19 are documented and b'\\x05' collision is confirmed"
    - "All new SamplingParams fields between v0.14 and v0.19 are enumerated"
    - "v0.19 InputBatch.block_table API (MultiGroupBlockTable) is documented with access patterns"
    - "v0.19 schedule() structure is mapped with loop layout and hook insertion points"
    - "All v0.19 EngineCoreClient subclasses are enumerated with their line numbers and purposes"
  artifacts:
    - path: ".planning/phases/01-groundwork-audit/01-AUDIT-FINDINGS.md"
      provides: "All five audit findings in factual format"
      contains: "AUDIT-01"
      min_lines: 80
  key_links: []
---

<objective>
Audit v0.19.0 internals and document all structural differences relevant to eviction code adaptation.

Purpose: The upgrade requires adapting eviction hooks to v0.19 internals (scheduler, model runner, IPC). Without precise knowledge of what changed, Phase 2-3 work would be guesswork. Per D-05 and D-06, findings go into a dedicated doc with factual summaries and code references — no recommendations.

Output: `.planning/phases/01-groundwork-audit/01-AUDIT-FINDINGS.md` containing all five audit findings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-groundwork-audit/01-CONTEXT.md
@.planning/phases/01-groundwork-audit/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit v0.19.0 internals and produce AUDIT-FINDINGS.md</name>
  <files>.planning/phases/01-groundwork-audit/01-AUDIT-FINDINGS.md</files>
  <read_first>
    - .planning/phases/01-groundwork-audit/01-RESEARCH.md (contains pre-research findings for all 5 audits — lines 246-313)
    - .planning/phases/01-groundwork-audit/01-CONTEXT.md (D-05, D-06, D-07 control output format and access method)
  </read_first>
  <action>
**Per D-07:** Access v0.19 source using `git show v0.19.0:path/to/file` commands. The v0.19.0 tag (commit 2a69949) should already be fetched. If not, run: `git fetch upstream refs/tags/v0.19.0:refs/tags/v0.19.0`

**Per D-05:** Write findings to `.planning/phases/01-groundwork-audit/01-AUDIT-FINDINGS.md`

**Per D-06:** Each finding uses the format:
- What was found (factual summary)
- Exact file paths and line numbers in v0.19.0
- Implications for eviction code
- No recommendations

**Verify each finding against actual v0.19.0 source** (do NOT just copy from RESEARCH.md — confirm with `git show`):

**AUDIT-01: EngineCoreRequestType byte values**
Run: `git show v0.19.0:vllm/v1/engine/__init__.py | grep -n "class EngineCoreRequestType" -A 20`
Document: Every byte value assignment, confirm `WAKEUP = b"\x05"` collision with fork's `UPDATE_MASK = b'\x05'`, and identify the next free byte value.

**AUDIT-02: SamplingParams field differences**
Run: `git show v0.19.0:vllm/sampling_params.py | grep -n "class SamplingParams" -A 200`
Compare against current fork's SamplingParams. Document: all added fields (with types and defaults), all removed fields, all changed fields. Note that fork's `enable_l2_norms` and `l2_norm_layers` must be re-appended AFTER the last v0.19 field.

**AUDIT-03: InputBatch.block_table API**
Run: `git show v0.19.0:vllm/v1/worker/block_table.py`
Document: `MultiGroupBlockTable` class structure, how to access individual `BlockTable` objects, numpy array access pattern (`block_table.np` via `CpuGpuBuffer`), device tensor access. Note incompatibility with fork's `.block_tables` (plural list) access.

**AUDIT-04: schedule() structure**
Run: `git show v0.19.0:vllm/v1/core/sched/scheduler.py | head -1000`
Document: method signature and line number, the two main loops (RUNNING/WAITING), `SchedulerOutput` construction site (fields and line number), `_update_after_schedule` call site, `update_from_output` location, `_free_request` location. Identify where eviction hooks (`_process_evictions`, `update_request_mask`) should insert.

**AUDIT-05: EngineCoreClient subclasses**
Run: `git show v0.19.0:vllm/v1/engine/core_client.py | grep -n "class "`
Document: All 7 classes with line numbers and purposes. Note which are new vs existing. For each, note whether it has IPC (ZMQ) or direct access, since this affects how eviction methods must be implemented.

**Document structure:**
```markdown
# Phase 1: Audit Findings

**Audited:** {date}
**v0.19.0 ref:** commit 2a69949

## AUDIT-01: EngineCoreRequestType Byte Values
{findings}

## AUDIT-02: SamplingParams Field Differences (v0.14 -> v0.19)
{findings}

## AUDIT-03: InputBatch Block Table API (v0.19)
{findings}

## AUDIT-04: schedule() Structure (v0.19)
{findings}

## AUDIT-05: EngineCoreClient Subclasses (v0.19)
{findings}
```
  </action>
  <verify>
    <automated>cd /home/users/ntu/hong0259/vllm-thought-eviction && python -c "
with open('.planning/phases/01-groundwork-audit/01-AUDIT-FINDINGS.md') as f:
    content = f.read()
for audit_id in ['AUDIT-01', 'AUDIT-02', 'AUDIT-03', 'AUDIT-04', 'AUDIT-05']:
    assert f'## {audit_id}' in content, f'{audit_id} section missing'
assert 'WAKEUP' in content, 'AUDIT-01 must mention WAKEUP collision'
assert 'b\"\\x05\"' in content or \"b'\\\\x05'\" in content or 'x05' in content, 'AUDIT-01 must mention byte value'
assert 'MultiGroupBlockTable' in content, 'AUDIT-03 must mention MultiGroupBlockTable'
assert 'SchedulerOutput' in content, 'AUDIT-04 must mention SchedulerOutput'
assert 'DPLBAsyncMPClient' in content, 'AUDIT-05 must mention new client'
assert len(content.splitlines()) >= 80, f'Document too short ({len(content.splitlines())} lines, need >= 80)'
print('AUDIT-FINDINGS.md verified OK — all 5 sections present with key content')
"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-groundwork-audit/01-AUDIT-FINDINGS.md` exists
    - File contains sections `## AUDIT-01` through `## AUDIT-05`
    - AUDIT-01 section mentions `WAKEUP` and the `\x05` byte collision
    - AUDIT-02 section lists added, removed, and changed SamplingParams fields
    - AUDIT-03 section mentions `MultiGroupBlockTable` and access patterns
    - AUDIT-04 section mentions `SchedulerOutput` construction site with line number
    - AUDIT-05 section lists all 7 client classes including `DPLBAsyncMPClient`
    - Document is at least 80 lines long
    - All findings are verified against actual `git show v0.19.0:` output (not just copied from RESEARCH.md)
  </acceptance_criteria>
  <done>All five audit findings documented with factual summaries, exact v0.19.0 file paths and line numbers, and implications for eviction code. No recommendations included (per D-06).</done>
</task>

</tasks>

<verification>
1. `01-AUDIT-FINDINGS.md` exists and has all 5 sections
2. Each section contains file paths and line numbers from v0.19.0
3. Key structural facts are present (collision byte, new fields, new classes, API changes)
</verification>

<success_criteria>
- 01-AUDIT-FINDINGS.md contains verified findings for all 5 audit items
- Each finding includes exact v0.19.0 file paths and line numbers
- Format follows D-06 (factual summary + code references, no recommendations)
- Findings are verified against actual v0.19.0 source (not just copied from research)
</success_criteria>

<output>
After completion, create `.planning/phases/01-groundwork-audit/01-02-SUMMARY.md`
</output>

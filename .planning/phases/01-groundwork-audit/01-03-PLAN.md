---
phase: 01-groundwork-audit
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tests/eviction/test_smoke.py
autonomous: true
requirements:
  - CLEAN-03

must_haves:
  truths:
    - "A functional smoke test exists that starts vLLM server, sends a streaming chat completion with eviction_params, and checks L2 norms, eviction events, and eviction statistics"
    - "The smoke test uses the deepseek-8b model at $HOME/scratch/models/deepseek-8b"
    - "The smoke test is designed to run on GPU compute nodes (not login nodes)"
  artifacts:
    - path: "tests/eviction/test_smoke.py"
      provides: "Server-level functional smoke test for eviction pipeline"
      contains: "eviction_params"
      min_lines: 100
  key_links:
    - from: "tests/eviction/test_smoke.py"
      to: "vllm/entrypoints/openai/api_server"
      via: "HTTP request to /v1/chat/completions"
      pattern: "chat/completions"
    - from: "tests/eviction/test_smoke.py"
      to: "vllm/entrypoints/openai/chat_completion/serving.py"
      via: "eviction_params in request body triggers EvictionOrchestrator"
      pattern: "eviction_params"
---

<objective>
Write a functional server-level smoke test that drives the full eviction pipeline end-to-end.

Purpose: Per D-01 through D-04 and D-09, this test validates that the eviction pipeline actually works at runtime — not just that source code looks correct. It must pass on the current v0.14 codebase (establishing a baseline per D-04) and will be used to verify bug fixes and the final upgrade.

Output: `tests/eviction/test_smoke.py` — a pytest-compatible test that starts a vLLM server, sends a streaming chat completion with eviction parameters, and verifies L2 norms, eviction events, and final statistics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-groundwork-audit/01-CONTEXT.md
@.planning/phases/01-groundwork-audit/01-RESEARCH.md

@vllm/entrypoints/openai/chat_completion/protocol.py
@vllm/entrypoints/openai/chat_completion/serving.py
@vllm/thought_eviction/orchestrator.py
@tests/eviction/test_serving_integration.py
@tests/eviction/test_orchestrator.py

<interfaces>
<!-- Key types the smoke test must exercise -->

From vllm/entrypoints/openai/chat_completion/protocol.py:
- EvictionParams model with fields: strategy (str), keep_ratio (float), prune_after_tokens (int), trigger_mode (str), retention_window (int), l2_norm_layers (list[int])
- ChatCompletionRequest accepts optional eviction_params field

From vllm/entrypoints/openai/chat_completion/serving.py:
- When eviction_params present and stream=True, response wraps with EvictionOrchestrator
- Final SSE chunk includes eviction statistics (evicted_token_count, strategy)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write functional smoke test for eviction pipeline</name>
  <files>tests/eviction/test_smoke.py</files>
  <read_first>
    - vllm/entrypoints/openai/chat_completion/protocol.py (EvictionParams fields, ChatCompletionRequest.eviction_params)
    - vllm/entrypoints/openai/chat_completion/serving.py (how eviction wrapping works, what the final SSE chunk contains)
    - vllm/thought_eviction/orchestrator.py (wrap_stream method, eviction statistics format)
    - tests/eviction/test_serving_integration.py (existing test patterns, NOT to copy inspect-based approach but to understand module structure)
    - tests/eviction/test_orchestrator.py (see factory helpers and module docstring patterns)
  </read_first>
  <action>
**Per D-01:** This is a server integration test — start actual vLLM server, send HTTP requests.
**Per D-02:** Use model at `$HOME/scratch/models/deepseek-8b`.
**Per D-03:** Passing criteria: L2 norms returned, at least one eviction event, final SSE chunk has eviction statistics.
**Per D-04:** Designed to establish baseline on v0.14 first.

Create `tests/eviction/test_smoke.py` with the following structure:

**Module docstring:** Document that this is a functional smoke test requiring GPU and the deepseek-8b model.

**Pytest marker:** Add `pytestmark = pytest.mark.skipif(not torch.cuda.is_available(), reason="Requires GPU")` at module level so it auto-skips on login nodes.

**Constants:**
```python
MODEL_PATH = os.path.expandvars("$HOME/scratch/models/deepseek-8b")
SERVER_HOST = "127.0.0.1"
SERVER_PORT = 8192  # non-default to avoid conflicts
HEALTH_TIMEOUT = 300  # seconds — model loading is slow
REQUEST_TIMEOUT = 120  # seconds for completion
```

**Server management:**
Use `subprocess.Popen` to start:
```python
cmd = [
    sys.executable, "-m", "vllm.entrypoints.openai.api_server",
    "--model", MODEL_PATH,
    "--host", SERVER_HOST,
    "--port", str(SERVER_PORT),
    "--dtype", "auto",
    "--max-model-len", "4096",
    "--trust-remote-code",
]
```
- Health check: poll `GET /health` until 200 or timeout
- Cleanup: `process.terminate()` then `process.wait(timeout=30)` then `process.kill()` if needed
- Use a pytest fixture (`@pytest.fixture(scope="module")`) for server lifecycle

**Test function `test_eviction_smoke`:**
1. Build request body:
```python
body = {
    "model": MODEL_PATH,
    "messages": [{"role": "user", "content": "Solve step by step: What is the sum of the first 20 prime numbers?"}],
    "stream": True,
    "max_tokens": 2048,
    "eviction_params": {
        "strategy": "global",
        "keep_ratio": 0.5,
        "prune_after_tokens": 50,
        "trigger_mode": "periodic",
        "l2_norm_layers": [0, 1, 2],
    }
}
```
2. Send POST to `http://{host}:{port}/v1/chat/completions` with `stream=True`
3. Parse SSE chunks line by line (each line starting with `data: ` contains JSON)
4. Collect all chunks into a list
5. Assert:
   - At least one chunk received (response not empty)
   - Final chunk (the one with `"finish_reason"` != null or `[DONE]`) — look for eviction statistics in the response. Check for `eviction_stats` or `evicted_token_count` in the final data chunk.
   - L2 norms: Check if any chunk or the response metadata contains L2 norm data (look for `l2_norms` or `new_l2_norms` key).
   - Eviction event: Check for `evicted_token_count > 0` in the final statistics.

**Use `import regex as re`** (not `import re`) per project convention.

**HTTP client:** Use `urllib.request` from stdlib (no external dependency needed). For streaming, use `http.client.HTTPConnection` or `urllib.request.urlopen` and read line by line. Alternatively, check if `requests` or `httpx` is available in the venv and use it if so; fall back to urllib.

**Error handling:** If server fails to start or health check times out, skip with `pytest.skip("Server failed to start")` rather than failing — this makes the test robust on nodes without GPU or without the model.

**Important:** The prompt MUST trigger extended `<think>` reasoning from DeepSeek. A math problem works well. The eviction with `prune_after_tokens: 50` and `keep_ratio: 0.5` should trigger eviction once the think block exceeds 50 tokens.
  </action>
  <verify>
    <automated>cd /home/users/ntu/hong0259/vllm-thought-eviction && python -c "
import ast
with open('tests/eviction/test_smoke.py') as f:
    src = f.read()
tree = ast.parse(src)

# Check key elements exist
funcs = [n.name for n in ast.walk(tree) if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef))]
assert 'test_eviction_smoke' in funcs, 'test_eviction_smoke function not found'

# Check content
assert 'eviction_params' in src, 'eviction_params not in request body'
assert 'deepseek-8b' in src or 'MODEL_PATH' in src, 'model path not referenced'
assert 'subprocess' in src, 'subprocess not used for server management'
assert '/v1/chat/completions' in src, 'chat completions endpoint not called'
assert 'stream' in src, 'streaming not configured'
assert 'import regex as re' in src or 'regex' not in src, 'must use regex not re (or not use regex at all)'
assert 'import re' not in src.replace('import regex as re', ''), 'must not import re directly'
assert 'cuda' in src.lower() or 'gpu' in src.lower(), 'GPU skip condition not present'
assert 'keep_ratio' in src, 'keep_ratio not in eviction_params'
assert 'prune_after_tokens' in src, 'prune_after_tokens not in eviction_params'
assert len(src.splitlines()) >= 100, f'File too short ({len(src.splitlines())} lines)'

print('Smoke test structure verified OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/eviction/test_smoke.py` exists with at least 100 lines
    - File contains `test_eviction_smoke` function
    - Request body includes `eviction_params` with `strategy`, `keep_ratio`, `prune_after_tokens`, `trigger_mode`, `l2_norm_layers` fields
    - Server started via `subprocess.Popen` with `vllm.entrypoints.openai.api_server`
    - Model path references `deepseek-8b`
    - Streaming enabled (`"stream": True`)
    - POST sent to `/v1/chat/completions`
    - Test skips on non-GPU nodes (checks `torch.cuda.is_available()`)
    - Does NOT use `import re` (uses `import regex as re` if regex needed)
    - Assertions check for eviction statistics and L2 norm presence
  </acceptance_criteria>
  <done>Functional smoke test exists that starts vLLM server with deepseek-8b, sends streaming chat completion with eviction_params, and verifies L2 norms returned, eviction event occurred, and final SSE chunk contains eviction statistics. Test auto-skips on non-GPU nodes.</done>
</task>

</tasks>

<verification>
1. `python -m py_compile tests/eviction/test_smoke.py` — file parses without syntax errors
2. `python -c "import ast; ast.parse(open('tests/eviction/test_smoke.py').read())"` — valid Python
3. On a GPU node: `pytest tests/eviction/test_smoke.py -v` should pass (or skip gracefully if model not found)
4. On login node: `pytest tests/eviction/test_smoke.py -v` should show SKIPPED (no GPU)
</verification>

<success_criteria>
- tests/eviction/test_smoke.py exists and is syntactically valid Python
- Contains server management (start, health check, cleanup)
- Contains streaming chat completion request with eviction_params
- Contains assertions for L2 norms, eviction events, eviction statistics
- Skips gracefully on non-GPU nodes
</success_criteria>

<output>
After completion, create `.planning/phases/01-groundwork-audit/01-03-SUMMARY.md`
</output>

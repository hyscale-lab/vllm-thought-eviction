---
phase: 01-groundwork-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vllm/entrypoints/api_server.py
  - vllm/v1/engine/core_client.py
  - vllm/v1/attention/l2_norm_cache.py
  - vllm/v1/worker/gpu_model_runner.py
  - vllm/v1/attention/backends/flash_attn.py
  - tests/eviction/test_no_eviction_guard.py
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02

must_haves:
  truths:
    - "api_server.py no longer crashes on startup due to Pydantic v1 parse_obj call"
    - "InprocClient.update_request_mask_async is callable and delegates to scheduler"
    - "InprocClient.get_request_l2_norms_async is callable"
    - "L2NormCache has only one singleton mechanism (module-level global)"
    - "FlashAttentionMetadata has no compute_l2_norms or request_ids fields"
    - "_compute_l2_norms sorts layers numerically, not lexicographically"
    - "gpu_model_runner.py has no duplicate get_l2_norm_cache import"
    - "l2_norm_cache.py has no dead update_norms method"
  artifacts:
    - path: "vllm/entrypoints/api_server.py"
      provides: "Fixed Pydantic v2 model_validate call"
      contains: "model_validate"
    - path: "vllm/v1/engine/core_client.py"
      provides: "InprocClient eviction method implementations"
      contains: "update_request_mask_async"
    - path: "vllm/v1/attention/l2_norm_cache.py"
      provides: "Clean single-singleton L2NormCache"
      contains: "get_l2_norm_cache"
    - path: "vllm/v1/worker/gpu_model_runner.py"
      provides: "Fixed layer sorting, no duplicate import"
      contains: "key=lambda"
    - path: "vllm/v1/attention/backends/flash_attn.py"
      provides: "Clean FlashAttentionMetadata without dead fields"
  key_links:
    - from: "vllm/v1/engine/core_client.py"
      to: "vllm/v1/core/sched/scheduler.py"
      via: "InprocClient.engine_core.scheduler.update_request_mask()"
      pattern: "self\\.engine_core\\.scheduler\\.update_request_mask"
    - from: "vllm/v1/worker/gpu_model_runner.py"
      to: "vllm/v1/attention/l2_norm_cache.py"
      via: "single import of get_l2_norm_cache"
      pattern: "from vllm\\.v1\\.attention\\.l2_norm_cache import get_l2_norm_cache"
---

<objective>
Fix pre-existing bugs and clean up dead/broken code in the v0.14 eviction codebase.

Purpose: These bugs would crash the server or produce incorrect results during the upgrade. Fixing them now (per D-08, D-09) establishes a clean baseline before any merge work. The cleanup items (D-11 through D-15) remove dead code and fix latent bugs that would complicate Phase 2-3 adaptation.

Output: Six files patched with minimal targeted fixes. No behavioral changes beyond crash prevention and correctness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-groundwork-audit/01-CONTEXT.md
@.planning/phases/01-groundwork-audit/01-RESEARCH.md

@vllm/entrypoints/api_server.py
@vllm/v1/engine/core_client.py
@vllm/v1/attention/l2_norm_cache.py
@vllm/v1/worker/gpu_model_runner.py
@vllm/v1/attention/backends/flash_attn.py
@tests/eviction/test_no_eviction_guard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CLEAN-01 and CLEAN-02 crash bugs</name>
  <files>vllm/entrypoints/api_server.py, vllm/v1/engine/core_client.py</files>
  <read_first>
    - vllm/entrypoints/api_server.py (see line 68 for parse_obj crash)
    - vllm/v1/engine/core_client.py (see lines 183-195 for base class stubs, lines 273-358 for InprocClient)
    - vllm/v1/core/sched/scheduler.py (verify update_request_mask method exists on scheduler)
    - vllm/v1/engine/core.py (verify EngineCore.scheduler attribute exists)
  </read_first>
  <action>
**CLEAN-01 (per D-08 — strict minimal fix):**
In `vllm/entrypoints/api_server.py` line 68, replace:
```python
mask_request = UpdateMaskRequest.parse_obj(json_request)
```
with:
```python
mask_request = UpdateMaskRequest.model_validate(json_request)
```
No other changes to this file.

**CLEAN-02 (per D-08 — strict minimal fix):**
In `vllm/v1/engine/core_client.py`, add three methods to the `InprocClient` class (after the existing `dp_engines_running` method, before the class ends):

```python
async def update_request_mask_async(
    self, request_id: str, evictable_token_ranges: list[tuple[int, int]]
):
    self.engine_core.scheduler.update_request_mask(
        request_id, evictable_token_ranges
    )

def update_request_mask(
    self, request_id: str, evictable_token_ranges: list[tuple[int, int]]
):
    self.engine_core.scheduler.update_request_mask(
        request_id, evictable_token_ranges
    )

async def get_request_l2_norms_async(
    self, request_id: str, start_index: int = 0
) -> Optional[List[float]]:
    from vllm.v1.attention.l2_norm_cache import get_l2_norm_cache
    cache = get_l2_norm_cache()
    norms = cache.get_norms(request_id, start_index)
    return norms
```

Ensure `Optional` and `List` are imported from `typing` at the top of the file (check if already present; add only if missing).
  </action>
  <verify>
    <automated>cd /home/users/ntu/hong0259/vllm-thought-eviction && python -c "
import ast, sys
# Verify CLEAN-01: model_validate in api_server.py
with open('vllm/entrypoints/api_server.py') as f:
    src = f.read()
assert 'model_validate' in src, 'CLEAN-01: model_validate not found'
assert 'parse_obj' not in src, 'CLEAN-01: parse_obj still present'
# Verify CLEAN-02: InprocClient methods
with open('vllm/v1/engine/core_client.py') as f:
    src = f.read()
tree = ast.parse(src)
for node in ast.walk(tree):
    if isinstance(node, ast.ClassDef) and node.name == 'InprocClient':
        methods = [n.name for n in node.body if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef))]
        assert 'update_request_mask_async' in methods, 'CLEAN-02: update_request_mask_async missing'
        assert 'update_request_mask' in methods, 'CLEAN-02: update_request_mask missing'
        assert 'get_request_l2_norms_async' in methods, 'CLEAN-02: get_request_l2_norms_async missing'
        break
print('CLEAN-01 and CLEAN-02 verified OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `vllm/entrypoints/api_server.py` contains `model_validate` and does NOT contain `parse_obj`
    - `vllm/v1/engine/core_client.py` InprocClient class contains `update_request_mask_async`, `update_request_mask`, and `get_request_l2_norms_async` methods
    - InprocClient methods delegate to `self.engine_core.scheduler.update_request_mask`
  </acceptance_criteria>
  <done>api_server.py uses Pydantic v2 model_validate; InprocClient implements all three eviction delegation methods</done>
</task>

<task type="auto">
  <name>Task 2: Clean up dead code and fix latent bugs (D-11 through D-15)</name>
  <files>vllm/v1/worker/gpu_model_runner.py, vllm/v1/attention/backends/flash_attn.py, vllm/v1/attention/l2_norm_cache.py, tests/eviction/test_no_eviction_guard.py</files>
  <read_first>
    - vllm/v1/worker/gpu_model_runner.py (lines 125-175 for imports, line 1230 for sorting, lines 1895-1940 for extra_attn_metadata_args)
    - vllm/v1/attention/backends/flash_attn.py (lines 215-225 for dead fields, lines 320-335 for build params, lines 500-510 for assignment)
    - vllm/v1/attention/l2_norm_cache.py (lines 80-100 for dual singleton, lines 216-240 for dead method, lines 345-356 for module-level singleton)
    - tests/eviction/test_no_eviction_guard.py (line 48 for _instance reference)
  </read_first>
  <action>
**D-11: Remove duplicate import in gpu_model_runner.py**
Delete line 171 (`from vllm.v1.attention.l2_norm_cache import get_l2_norm_cache`). Keep the first import at line 129.

**D-12: Remove dead FlashAttentionMetadata fields**
In `vllm/v1/attention/backends/flash_attn.py`:
1. Remove `compute_l2_norms: bool = False` (line 220) and `request_ids: Optional[list[str]] = None` (line 221) from the `FlashAttentionMetadata` class.
2. Remove `request_ids: Optional[list[str]] = None,` (line 327) and `compute_l2_norms: bool = False` (line 328) from the `build` classmethod parameters.
3. Remove `request_ids=request_ids,` (line 506) and `compute_l2_norms=compute_l2_norms,` (line 507) from the `build` return constructor call.

In `vllm/v1/worker/gpu_model_runner.py`:
1. Remove the lines that populate `extra_attn_metadata_args['request_ids']` (line 1917) and `extra_attn_metadata_args['compute_l2_norms']` (line 1918).
2. Keep `extra_attn_metadata_args` dict and its `**extra_attn_metadata_args` spread — other fields may use it.

**D-13: Fix layer sorting in _compute_l2_norms**
In `vllm/v1/worker/gpu_model_runner.py` line 1230, replace:
```python
idx_to_name = sorted(attn_metadata_dict.keys())
```
with:
```python
idx_to_name = sorted(attn_metadata_dict.keys(),
                     key=lambda name: int(name.rsplit('.', 1)[-1]))
```

**D-14: Delete dead update_norms method**
In `vllm/v1/attention/l2_norm_cache.py`, delete the entire `update_norms` method (lines 216-239, from `def update_norms(` through the end of the method body before `def update_norms_batch(`).

**D-15: Fix dual singleton pattern**
In `vllm/v1/attention/l2_norm_cache.py`:
1. Remove `_instance: Optional['L2NormCache'] = None` (line 86)
2. Remove `_lock = threading.Lock()` (line 87)
3. Remove the entire `__new__` method (lines 89-95)
4. Rename `_init_cache(self)` to `__init__(self)` (line 97) so normal construction works.
5. The module-level `_l2_norm_cache` global and `get_l2_norm_cache()` function (lines 346-355) remain unchanged — they become the ONLY singleton mechanism.

Also update `tests/eviction/test_no_eviction_guard.py` line 46-49. Replace `_reset_cache` method:
```python
def _reset_cache(self):
    import vllm.v1.attention.l2_norm_cache as cache_mod
    cache_mod._l2_norm_cache = None
    return cache_mod.get_l2_norm_cache()
```
This resets via the module-level global instead of the removed `_instance` class attribute.
  </action>
  <verify>
    <automated>cd /home/users/ntu/hong0259/vllm-thought-eviction && python -c "
import ast
# D-11: no duplicate import
with open('vllm/v1/worker/gpu_model_runner.py') as f:
    lines = f.readlines()
import_count = sum(1 for l in lines if 'from vllm.v1.attention.l2_norm_cache import get_l2_norm_cache' in l)
assert import_count == 1, f'D-11: expected 1 import, found {import_count}'

# D-12: no dead fields in FlashAttentionMetadata
with open('vllm/v1/attention/backends/flash_attn.py') as f:
    src = f.read()
tree = ast.parse(src)
for node in ast.walk(tree):
    if isinstance(node, ast.ClassDef) and node.name == 'FlashAttentionMetadata':
        fields = [n.target.id for n in node.body if isinstance(n, ast.AnnAssign) and isinstance(n.target, ast.Name)]
        assert 'compute_l2_norms' not in fields, 'D-12: compute_l2_norms still in class'
        assert 'request_ids' not in fields, 'D-12: request_ids still in class'
        break

# D-13: numeric sort
with open('vllm/v1/worker/gpu_model_runner.py') as f:
    src = f.read()
assert 'key=lambda' in src and 'rsplit' in src, 'D-13: numeric sort not found'

# D-14: no update_norms method (but update_norms_batch still exists)
with open('vllm/v1/attention/l2_norm_cache.py') as f:
    src = f.read()
assert 'def update_norms_batch(' in src, 'update_norms_batch should still exist'
# Check update_norms is gone (but not update_norms_batch)
import regex as re
standalone = re.findall(r'def update_norms\(', src)
assert len(standalone) == 0, f'D-14: update_norms still present ({len(standalone)} occurrences)'

# D-15: no _instance, no __new__, has __init__
assert '_instance' not in src, 'D-15: _instance still in l2_norm_cache.py'
assert 'def __new__' not in src, 'D-15: __new__ still in l2_norm_cache.py'
assert 'def __init__' in src, 'D-15: __init__ not found'
assert '_l2_norm_cache' in src, 'D-15: module-level singleton missing'

# D-15 test update
with open('tests/eviction/test_no_eviction_guard.py') as f:
    tsrc = f.read()
assert '_instance' not in tsrc, 'D-15: test still references _instance'
assert 'cache_mod._l2_norm_cache = None' in tsrc, 'D-15: test not using module-level reset'

print('All cleanup items (D-11 through D-15) verified OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `gpu_model_runner.py` has exactly 1 line containing `from vllm.v1.attention.l2_norm_cache import get_l2_norm_cache`
    - `flash_attn.py` FlashAttentionMetadata class has no `compute_l2_norms` or `request_ids` fields
    - `gpu_model_runner.py` contains `key=lambda name: int(name.rsplit` in the _compute_l2_norms method
    - `l2_norm_cache.py` does NOT contain `def update_norms(` (standalone, not update_norms_batch)
    - `l2_norm_cache.py` does NOT contain `_instance` or `__new__`
    - `l2_norm_cache.py` contains `def __init__(self)` and `_l2_norm_cache` module global
    - `tests/eviction/test_no_eviction_guard.py` uses `cache_mod._l2_norm_cache = None` instead of `_instance`
  </acceptance_criteria>
  <done>All six cleanup items (D-11 through D-15) applied. Dead code removed, layer sorting fixed to numeric, singleton unified to module-level only, test updated.</done>
</task>

</tasks>

<verification>
1. `python -c "from vllm.entrypoints.api_server import app"` does not raise ImportError or AttributeError for parse_obj
2. AST verification confirms all InprocClient methods exist
3. All D-11 through D-15 changes verified by grep/AST checks above
4. Existing tests still parse: `python -m py_compile tests/eviction/test_no_eviction_guard.py`
</verification>

<success_criteria>
- api_server.py uses model_validate (not parse_obj)
- InprocClient has update_request_mask_async, update_request_mask, get_request_l2_norms_async
- No duplicate imports, no dead fields, numeric layer sort, single singleton, no dead methods
- All modified files parse without syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-groundwork-audit/01-01-SUMMARY.md`
</output>
